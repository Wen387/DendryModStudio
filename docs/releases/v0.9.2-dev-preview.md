# v0.9.2 Dev Preview Release Prep

Date: 2026-05-01
Status: draft preparation for a GitHub pre-release, not a signed public release.

## Current Verification

- The repository checks pass with `npm run check:ci`.
- The latest GitHub Checks run for commit `bf4a120` completed successfully:
  `https://github.com/Wen387/DendryModStudio/actions/runs/25220555420`.
- The static update notice manifest is reachable from GitHub raw content:
  `https://raw.githubusercontent.com/Wen387/DendryModStudio/main/tools/project_map/desktop/update_manifest.json`.
- The update notice manifest includes both `en` and `zh-Hant` title/body copy.
- The root `package-lock.json` is tracked so clean clones and CI can use `npm ci`.
- The release workflow can publish Windows `.exe`, Linux AppImage, and Linux
  Deb assets when run with `publish_release` and a release tag.

## Release Candidate Gate

Run these checks from a fresh clone before attaching packages to a release:

```bash
npm ci --ignore-scripts
npm run check:ci
```

Then run desktop checks:

```bash
cd tools/project_map/desktop
npm ci
npm run smoke
npm run doctor
```

On Linux, a local packaging check can also build the AppImage and Deb:

```bash
npm run dist:linux
```

Windows artifacts should be built by the GitHub `Desktop Release` workflow.

Before calling the package ready for invitee testing, manually walk the player
path from a clean app data state:

1. Open the packaged desktop app.
2. Use Quick Start and load the bundled Demo Template.
3. Inspect the demo event, player text, choices, effects, and variable condition.
4. Create one proposal and save it to My Changes.
5. Reopen the saved draft and run Review & Apply dry-run.
6. Apply only guarded operations in a disposable project copy.
7. Open Runtime Preview and confirm the comparison page works.
8. Check the update notice button/banner in both English and Traditional Chinese UI.

## Draft GitHub Release Notes

Create the GitHub release as a pre-release until clean-machine QA is complete.
Use `v0.9.2-preview.1` as the draft tag only after the package artifacts have
been rebuilt and tested.

Attach these rebuilt artifacts when they pass the checks above:

- `DendryModStudio-win-x64.exe`
- `DendryModStudio-linux-x64.AppImage`
- `DendryModStudio-linux-x64.deb`

Use `tools/project_map/RELEASE_NOTES_v0.9.2.md` as the long-form release body.
Keep the release marked as a pre-release and describe the update notice system
as manual announcement/linking only, not automatic installation.

## Hold Before Public Stable Release

- Fresh Linux VM or clean user-account install test.
- At least one beginner-style test using the bundled Demo Template.
- Review of English and Traditional Chinese release copy.
- Confirmation that release artifacts do not include private notes, tokens,
  generated runtime output, or game project folders.
- Decision on Windows/macOS packaging scope.
