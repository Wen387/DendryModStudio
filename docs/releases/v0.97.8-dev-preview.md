# v0.97.8 Dev Preview Release Prep

Date: 2026-05-21
Status: local release-candidate preparation for a GitHub pre-release, pending final publication.

## Current Verification

- The root and desktop package metadata versions have been advanced to
  semver-compliant `0.97.8`; the user-facing release label is `v0.97.8`.
- The release workflow builds Windows `.exe`, Linux AppImage, and Linux Deb
  assets through the `Desktop Release` workflow.
- Release builds fetch the bundled Python runtime before packaging desktop
  artifacts.
- The bundled update manifest reports `latestVersion: 0.97.8`.
- v0.97.8 carries the route-understanding workbench, guided route-edit rules,
  player-path event editor layout, same-source-unit Review & Apply coalescing,
  Runtime Preview asset compatibility fixes, and the playable Starter Demo loop.

## Release Page and Link Status

Before publishing the preview, run `Desktop Release` manually on the current
`main` with `publish_release` disabled and confirm the uploaded artifacts
include:

- `DendryModStudio-win-x64.exe`
- `DendryModStudio-linux-x64.AppImage`
- `DendryModStudio-linux-x64.deb`

After the artifact-only run passes on the current `main`, rerun the workflow
with `publish_release` enabled and `release_tag` set to `v0.97.8-preview.1`,
or push the same `v*` tag, then confirm the README latest download links
resolve to those Release assets.

## Release Candidate Gate

Run these checks from a fresh clone before attaching packages to a release:

```bash
npm ci --ignore-scripts
npm run check:ci
npm run check:complexity
```

Then run desktop checks:

```bash
cd tools/project_map/desktop
npm ci
npm run smoke
npm run doctor
```

On Linux, a local packaging check can also build the AppImage and Deb:

```bash
npm run dist:linux
```

Windows artifacts should be built by the GitHub `Desktop Release` workflow.

## Draft GitHub Release Notes

Create the GitHub release as a pre-release until clean-machine QA is complete.
Use `v0.97.8-preview.1` as the draft tag only after the package artifacts have
been rebuilt and tested.

Attach these rebuilt artifacts when they pass the checks above:

- `DendryModStudio-win-x64.exe`
- `DendryModStudio-linux-x64.AppImage`
- `DendryModStudio-linux-x64.deb`

Use `tools/project_map/RELEASE_NOTES_v0.97.8.md` as the long-form release body.
Keep the release marked as a pre-release and describe the announcement/update
notice system as manual information feed/linking only, not automatic
installation.
