# Fixture Invariants

This file intentionally mentions INV-B001, INV-C002, INV-V001, and INV-V002 so the IslandSunrise profile can detect project-level rules in a tiny compatibility fixture.

The fixture is not the full game. It only encodes source conventions that Studio is allowed to depend on.
